# Exercise 02 - addNumbers

Troubleshoot and modify the existing function so that it returns the digit 2. Avoid "hard coding" the result and try to use variables `a` and `b` to get the correct output.

Currently, it is not returning the correct value.

## Hints

- You only need to edit the strings on line 7.


# Ejercicio 02 - addNumbers

Corrige y modifica la función existente para que devuelva el dígito 2. Evita escribir el resultado directamente en el código ("hard coding") e intenta utilizar las variables `a` y `b` para obtener el resultado correcto.

Actualmente, no devuelve el valor correcto.

## Pistas

- Solo necesitas editar las cadenas de texto en la línea 7.